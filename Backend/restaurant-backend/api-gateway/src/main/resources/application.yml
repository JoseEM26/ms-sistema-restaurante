server:
  port: 8080

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
          lower-case-service-id: true

      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins:
              - "http://localhost:4200"
              - "http://localhost:3000"
            allowedMethods:
              - GET
              - POST
              - PUT
              - PATCH
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: true
            maxAge: 3600

      routes:
        # ── API Docs sin JWT (Swagger agregado) ───────────────
        - id: auth-api-docs
          uri: lb://ms-auth-security
          predicates:
            - Path=/api/auth/v3/api-docs
          filters:
            - RewritePath=/api/auth/v3/api-docs, /v3/api-docs

        - id: maestros-api-docs
          uri: lb://ms-core-maestros
          predicates:
            - Path=/api/maestros/v3/api-docs
          filters:
            - RewritePath=/api/maestros/v3/api-docs, /v3/api-docs

        - id: ventas-api-docs
          uri: lb://ms-ventas
          predicates:
            - Path=/api/ventas/v3/api-docs
          filters:
            - RewritePath=/api/ventas/v3/api-docs, /v3/api-docs

        # ── Rutas de negocio ──────────────────────────────────
        - id: ms-auth-security
          uri: lb://ms-auth-security
          predicates:
            - Path=/api/auth/**

        - id: ms-core-maestros
          uri: lb://ms-core-maestros
          predicates:
            - Path=/api/maestros/**
          filters:
            - JwtAuthFilter

        - id: ms-ventas
          uri: lb://ms-ventas
          predicates:
            - Path=/api/ventas/**
          filters:
            - JwtAuthFilter

        - id: ms-reportes
          uri: lb://ms-reportes
          predicates:
            - Path=/api/reportes/**
          filters:
            - JwtAuthFilter

  data:
    redis:
      host: localhost
      port: 6379

eureka:
  instance:
    prefer-ip-address: true
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

jwt:
  secret: clave-secreta-restaurant-2024-muy-larga-para-seguridad

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway

springdoc:
  api-docs:
    path: /v3/api-docs
  swagger-ui:
    path: /swagger-ui.html
    urls:
      - name: Restaurant API - Todos los endpoints
        url: /v3/api-docs
    urls-primary-name: Restaurant API - Todos los endpoints
